# PA3 implementation plan

- Stage base commit: `58db569f36226e077d7d54c1f6bff3767eebde22`. Last reviewed commit: `58db569f36226e077d7d54c1f6bff3767eebde22`.
- Design/spec alignment and ownership: `PPTokenizer` streams typed phase-1–3 callbacks into `ControllingExpressionStream`; it retains only the current logical line's token/spelling records, evaluates, and discards them. `Parser` owns C++11 integer/character literal conversion, `defined`, precedence/associativity, static conditional result typing, short-circuiting and signed/unsigned `intmax_t`/`uintmax_t` behavior. Phase failures propagate to the CLI's nonzero exit; malformed lines and course-defined arithmetic traps print `error` locally. No rendered-token or IR roundtrip. Normal work is O(input bytes + tokens), with geometric per-line storage; precedence levels are fixed and nesting has a bounded recursion guard. The implementation is self-contained; later semantic/lowering design is unaffected.
- Completed groups: typed token/literal conversion, complete controlling-expression grammar, signed/unsigned evaluation, lazy evaluation and logical-line isolation. No course references or coverage changed. Implementation commit: `efe44af14`.
- Validation: `make test-pa3` 20/20; `make test-report-through-pa3` 100/100, 3/3 stages; focused `student.tests/pa3-semantic-groups.t` exact output; `perl scripts/cppgm_file_audit.pl --stage pa3 --paths dev/src` passes (20 files). Turn-start failures were 20/20; current failures are 0/20, with all required fixtures retained.
- Performance evidence (no optimization/performance-improvement claim): frozen `dev/ppexpr`, SHA-256 `b7b447123c70a2da473e21d4404c559204331af0c3ab27d02e2e0c63262e67df`, built with g++ 15.2.0, `-std=gnu++11 -Wall -O3`. Fixed 4,000,000-byte/200,000-line input SHA-256 `a27623dc1ae57710cdf480324fab963e8203540d2b5676433d62aefba6901380`; exact output is 200,000 `1` records plus `eof`, 400,004 bytes, SHA-256 `dbf17a3d345eb111023d6b231dd0eabe0f6e60f40e192f2e9848539bcbbb137b`. Seven wall times (seconds): 1.68, 1.67, 1.68, 1.66, 1.66, 1.66, 1.66 (median 1.66, range 1.66–1.68); peak RSS (KiB): 11268, 11248, 11264, 11268, 11252, 11180, 11232 (median 11252, max 11268). ppexpr `.text` 126,502 bytes (ELF section total 154,379). Raw evidence and frozen binary/input/output are in `$RALPH_ARTIFACT_DIR/pa3-final`. PA3 produces expression values, not generated programs, so generated-program runtime/text is inapplicable; no optimization budget or inherited numeric gate applies.
- Handoff ledger: no known unfinished implementation group after required and focused validation. Independent full-stage audit remains scheduled and is not waived; no reference correction was needed. Final required checks and clean-tree verification pass on the handoff; no reference correction was needed.
