# PA3 implementation plan

- Stage base commit: `58db569f36226e077d7d54c1f6bff3767eebde22`. Last reviewed commit: `58db569f36226e077d7d54c1f6bff3767eebde22`.
- Design/spec alignment and ownership: `PPTokenizer` streams typed phase-1–3 callbacks into `ControllingExpressionStream`; it retains only the current logical line's compact token/spelling records, then parses/evaluates and discards them. `Parser` owns C++11 integer/character literal conversion, `defined` handling, precedence/associativity, static conditional result typing, short-circuit evaluation and signed/unsigned `intmax_t`/`uintmax_t` behavior. Phase failures propagate to the CLI's nonzero exit; malformed expressions/literals and course-defined arithmetic traps print `error` for that line. No rendered-token or IR roundtrip. Normal work is O(input bytes + tokens), with geometric line storage; precedence levels are fixed and nesting has a bounded recursion guard. Later semantic/lowering design is unaffected.
- Completed implementation group: token/literal conversion, all controlling-expression grammar levels and line isolation. Course suite is 20/20; earlier stages 80/80; explicit `student.tests/pa3-semantic-groups.t` exact-output check passes. No reference outputs or course coverage changed.
- Remaining implementation groups: none identified after course and focused semantic validation. Independent audit must still examine this handoff; it is not a waiver of whole-stage review.
- Performance evidence: final frozen-binary measurements and artifact checks remain to be recorded. PA3 emits decimal results rather than program executables; generated-program runtime/text acceptance is inapplicable. No optimization benefit or numeric threshold is claimed.
- Handoff ledger: implementation is complete pending final measurement/validation and commit. Record owner/dataflow/complexity above; independent review questions remain distinct from implementation status.
